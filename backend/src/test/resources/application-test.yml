spring:
  datasource:
    url: jdbc:h2:mem:power_inspection;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  jwt:
    secret: test-secret-for-power-inspection
    ttl-seconds: 3600
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://127.0.0.1:5173
  model:
    mode: mock
  agent:
    orchestration:
      auto-execution-enabled: true
  robot:
    mode: simulation
    allow-registration: false
