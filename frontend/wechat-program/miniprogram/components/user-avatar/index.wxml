<view class="avatar" style="width:{{size}}rpx;height:{{size}}rpx;background:{{color}};font-size:{{fontSize}}rpx;">
  <image wx:if="{{avatarUrl}}" class="img" src="{{avatarUrl}}" mode="aspectFill" />
  <text wx:else class="initials">{{initials}}</text>
</view>
